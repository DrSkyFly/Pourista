<?xml version="1.0" encoding="utf-8"?>
<!--
  We hand outside only what gets shared: scale logs and brew pictures. Both folders are
  temporary, in the cache.
-->
<paths>
    <cache-path name="logs" path="logs/" />
    <cache-path name="share" path="share/" />
</paths>
