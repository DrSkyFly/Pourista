<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Request legacy Bluetooth permissions on versions older than API 31 (Android 12). -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- The pour step cues are echoed by vibration. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      None of this is required: a recipe with a step-by-step timer is brewed without a scale
      too. We declare it explicitly, otherwise the permissions above would put in "required"
      by themselves and the stores would hide the app from devices with no Bluetooth or no
      location sensors.
    -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <application
        android:name=".PouristaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pourista">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Pourista">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              A recipe file opened from outside: from a messenger, from mail, from a file
              manager. The system does not know our type, so we catch it two ways: by the type,
              if the sender set it, and by the file name.
            -->
            <intent-filter android:label="@string/recipe_file_label">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/vnd.pourista.recipe" />
            </intent-filter>
            <intent-filter android:label="@string/recipe_file_label">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <!--
                  The path pattern is literal: a name can hold any number of dots, and every
                  case has to be described separately.
                -->
                <data android:pathPattern=".*\\.pour" />
                <data android:pathPattern=".*\\..*\\.pour" />
                <data android:pathPattern=".*\\..*\\..*\\.pour" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.pour" />
                <data android:pathSuffix=".pour" tools:targetApi="31" />
            </intent-filter>

            <!-- "Share to Pourista" from a messenger. -->
            <intent-filter android:label="@string/recipe_file_label">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.pourista.recipe" />
                <data android:mimeType="application/json" />
            </intent-filter>
        </activity>

        <!--
          The scale log goes outside through "share", and for that the system needs access to
          the file: it will not share one by our own path from the cache.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
