# Build
/build
build/
.gradle/
.kotlin/
/captures
.externalNativeBuild
.cxx

# Local environment settings
local.properties
*.iml
/.idea

# The signing key and its passwords are not kept next to the project,
# but we close them off here just in case.
*.jks
*.keystore
keystore.properties

.DS_Store
